--COMENTÁRIO NA TABELA 
COMMENT ON TABLE TCONTRATO IS 'Informações de Contrato';

COMMENT ON COLUMN TCONTRATO.COD_CONTRATO IS 'Código do Contrato';

COMMENT ON COLUMN TCONTRATO.DATA IS 'Data de Admissão do Contrato';

--VERIFICAR COMENTÁRIOS

SELECT * FROM USER_COL_COMMENTS WHERE TABLE_NAME='TCONTRATO';

SELECT * FROM USER_TAB_COMMENTS WHERE TABLE_NAME='TCONTRATO';

--DESABILITAR COLUNA

ALTER TABLE TCONTRATO_VIP ADD DESABILITADA VARCHAR(10);

ALTER TABLE TCONTRATO_VIP SET UNUSED (DESABILITADA);

SELECT * FROM TCONTRATO_VIP;

SELECT * FROM USER_TABLES WHERE TABLE_NAME = 'TCONTRATO_VIP';

--EXCLUIR COLUNA DESABILITADA 

ALTER TABLE TCONTRATO_VIP DROP UNUSED COLUMNS;

--EXCLUI TODOS OS DADOS DA TABELA

SAVEPOINT HEHE;

TRUNCATE TABLE TCONTRATO_VIP;

SELECT * FROM TCONTRATO_VIP;

ROLLBACK;

