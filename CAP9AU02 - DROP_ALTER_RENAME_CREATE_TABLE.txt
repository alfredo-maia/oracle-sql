--CRIANDO TABELA

CREATE TABLE  TESTE
(
  CODIGO INTEGER NOT NULL PRIMARY KEY,
  DATA DATE DEFAULT SYSDATE
)

SELECT * FROM TESTE;

--VERIFICANDO TABELAS CRIADA PELO POR TODOS USUÁRIOS

SELECT * FROM ALL_TABLES;

--VERIFICANDO TABELAS CRIADAS PELO USUÁRIO

SELECT * FROM USER_TABLES;

INSERT INTO TESTE (CODIGO) VALUES (1);

--CRIANDO TABELA A PARTI DE SELECT

CREATE TABLE VIP 
  AS
  SELECT * FROM TCONTRATO 
    WHERE TOTAL > 500;
    
SELECT * FROM VIP;

--ADICIONAR CAMPO NA COLUNA

ALTER TABLE VIP ADD VALOR NUMBER(5,2);

--ALTERAR COLUNA

ALTER TABLE VIP MODIFY VALOR NUMBER(8,2);

--RENOMEAR TABELA

ALTER TABLE VIP RENAME COLUMN VALOR TO VALOR2;

--DELETAR COLUNA

ALTER TABLE VIP DROP COLUMN VALOR;

--EXCLUIR TABELA

DROP TABLE VIP;

--RENOMEAR

RENAME VIP TO TCONTRATO_VIP;

SELECT * FROM VIP;

SELECT * FROM USER_TABLES;